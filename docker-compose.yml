services:
  backend:
    build: ./backend
    container_name: virtualfinance-backend
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 10s
      timeout: 5s
      retries: 5

  frontend:
    build: ./frontend
    container_name: virtualfinance-frontend
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_healthy
